package com.somesing.www.common.web;

public class Constant {
	
	public enum ServiceResult {
        SUCCESS, 
        FAIL,
        DUPLICATE,
        NOT_FOUND,
        LEAVE,
        INVALID_TOKEN,
        NOT_MATCH,
        INVALID_PARAM,
        BAD_REQUEST,
        UNAUTHORIZED
    }
	
	public enum YesNoStatus {Y, N};
}
